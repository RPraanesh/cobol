pipeline {
   agent any
      stages {
      stage ('Build') {
      steps {
      echo 'Running Build Automation '
      sh 'sudo ./build.sh --no-daemon'
                       }
            }
       }
}
